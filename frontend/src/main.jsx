import React from "react";
import ReactDOM from "react-dom/client";
import MeetMind from "./MeetMind.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MeetMind />
  </React.StrictMode>
);
